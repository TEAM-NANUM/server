spring:
#  config:
#    activate:
#      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #/<IP>:<PORT>/<DB NAME>?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    url: jdbc:mysql://localhost:3306/nanum_db?serverTimezone=Asia/Seoul
    username: root
    password: mysql1234

  jpa:
    hibernate:
      ddl-auto: update
